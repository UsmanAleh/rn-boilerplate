/**
 * Enum for the different environments of the application.
 * @enum {string}
 * @readonly
 */
export enum AppEnvironment {
  Development = 'development',
  Staging = 'staging',
  Production = 'production',
}
